export default class User {
    constructor({id, name, avatar, occupation}) {
        this.id = id;
        this.name = name;
        this.avatar = avatar;
        this.occupation = occupation;
    }
}
